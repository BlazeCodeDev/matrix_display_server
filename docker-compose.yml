version: '3.8'

services:
  led-matrix:
    build: .
    container_name: led-matrix-server
    ports:
      - "3000:3000"
    volumes:
      - led-matrix-data:/app/data
    restart: unless-stopped
    environment:
      - PORT=3000

volumes:
  led-matrix-data:
